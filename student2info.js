const profilePicContainer = document.querySelector('.profile-pic-container');

profilePicContainer.addEventListener('click', function() {
  profilePicContainer.classList.toggle('fullscreen');
});
